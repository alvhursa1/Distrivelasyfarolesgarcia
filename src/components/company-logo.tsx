import { dancingScript } from '../app/fonts'

export function CompanyLogo() {
  return (
    <div className={`${dancingScript.className} text-white text-2xl md:text-3xl font-semibold animate-fade-down p-4 md:p-0`}>
      Distrivelas y faroles García
    </div>
  )
}

